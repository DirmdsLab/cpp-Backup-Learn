#include <iostream>
using namespace std;

int main(){
    //variabel
    int a, b; // bisa baca kan :v

    // deklarasi
    a = 10;

    // cin (memasukan nilai ke variabel)
    cin >> b; // b nilai variabel di atas 

    // hasilnya tinggal print
    cout << "Hasilnya : " << endl; // "" buat masukan seperti di code :v
    cout << a << endl; // bakal print variabel a
    cout << b << endl; // bakal print nilai yang kita input tadi
    return 0;
}
